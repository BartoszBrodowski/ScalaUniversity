@main
def fiszki: Unit = {

  // fruit("Apple") == true
  // fruit("banana") == false
val fruit = List("Apple", "Grape", "Orange", "Grape")

val napis = "Wobodobo"

val numbers = List(0, 1, 2, 3, 4)
println(numbers :+ 2)

}
